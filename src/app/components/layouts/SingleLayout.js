import React from "react";

// single page layout, for login/logout and such
export const SingleLayout = (props) => {
     return <div>
                {props.children}               
            </div>
};